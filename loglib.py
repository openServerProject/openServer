def info(text):
    logtype = 'info'.upper()
    print(f'{logtype}: {text}')

def warning(text):
    logtype = 'warning'.upper()
    print(f'{logtype}: {text}')

def error(text):
    logtype = 'error'.upper()
    print(f'{logtype}: {text}')
